export const ENTITY_REFERENCE_PATTERN = /\[\[([^\]]+)\]\]/g;

export const GENRE_REFERENCE_PATTERN = /^\[\[genre\/([^\]]+)\]\]$/;
export const ARTIST_REFERENCE_PATTERN = /^\[\[artist\/([^\]]+)\]\]$/;
export const LABEL_REFERENCE_PATTERN = /^\[\[label\/([^\]]+)\]\]$/;
export const RELEASE_REFERENCE_PATTERN = /^\[\[release\/([^\]]+)\]\]$/;
